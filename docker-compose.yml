services:
  pergamo:
    build:
      context: .
      dockerfile: Dockerfile.dev

    container_name: pergamo-angular

    ports:
      - "4200:4200"

    volumes:
      - .:/app
      - /app/node_modules
