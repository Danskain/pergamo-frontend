services:
  pergamo:
    build:
      context: .
      dockerfile: Dockerfile.dev

    container_name: pergamo-angular

    ports:
      - "4300:4300"

    volumes:
      - .:/app
      - /app/node_modules
